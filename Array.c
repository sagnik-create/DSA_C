#include <stdio.h>
int arrcr(int n){
  int arr[n];
  printf("Enter the element");
  for(int i=0; i<n; i++){
    scanf("%d",&arr[i]);
  }
  return arr[n];
}
int arrch(int useindex; int val){
    int value;
    value = useindex-1
    if (value == 0){
        return 0
    }
    arr[value] = val
}
int main(){
  int n,pos,ch;
  printf("Enter the size of Array");
  scanf("%d",&n);
  arrcr(n);
  printf("Enter the postion")
  scanf("")
  arrch(pos,ch);
}
